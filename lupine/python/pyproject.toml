[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lupine"
version = "1.0.0"
description = "Small PyTorch adapter helpers for LUPINE-backed CUDA devices"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["httpx[http2]>=0.28", "packaging>=24"]

[project.optional-dependencies]
torch = ["torch"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.hatch.build.targets.wheel]
packages = ["lupine"]
